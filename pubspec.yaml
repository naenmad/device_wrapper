name: device_wrapper
description: Wrap Flutter pages with realistic device frames (iPhone, iPad) for web/desktop preview. Auto-scaling, Dynamic Island, and mode switching.
version: 1.0.6
homepage: https://github.com/naenmad/device_wrapper
repository: https://github.com/naenmad/device_wrapper
issue_tracker: https://github.com/naenmad/device_wrapper/issues

topics:
  - ui
  - device-preview
  - responsive
  - web
  - simulator

environment:
  sdk: ">=2.17.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
